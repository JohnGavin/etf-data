name: Deploy Docs (Nix)

on:
  push:
    branches: [main]
    paths:
      - 'etfdata/**'
      - '.github/workflows/deploy-docs.yml'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v22
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - uses: cachix/cachix-action@v12
        with:
          name: rstats-on-nix

      # Optional: Push to user's cache if token exists
      - uses: cachix/cachix-action@v12
        continue-on-error: true
        with:
          name: johngavin
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Build and Push Environment
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
        run: |
          # Instantiate the shell derivation
          DRV=$(nix-instantiate etfdata/default.nix)
          
          # Build dependencies (this populates /nix/store)
          # We build regardless of push capability
          nix-store -r $DRV
          
          # Push the closure to Cachix ONLY if token is present
          if [[ -n "$CACHIX_AUTH_TOKEN" ]]; then
            echo "Pushing to johngavin cache..."
            nix-store -qR --include-outputs $DRV | cachix push johngavin
          else
            echo "Skipping Cachix push (no token found)."
          fi

      - name: Build Site
        run: |
          mkdir -p etfdata/local_lib_ci
          nix-shell etfdata/default.nix --run "
            Rscript -e '
              .libPaths(c("etfdata/local_lib_ci", .libPaths()));
              devtools::install("etfdata", upgrade = "never", quick = TRUE);
              pkgdown::build_site("etfdata", new_process = FALSE)
            '
          "
          mkdir -p _site
          cp -r etfdata/docs/* _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4